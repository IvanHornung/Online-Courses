//Types in Dart

//Every value has a 'type'
//Every variable has a type it can reference
//Once a variable has a 'type' associate, the variable's type cannot change
//We don't always have to annotate types, Dart can guess for us

